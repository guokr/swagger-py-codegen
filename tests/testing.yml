swagger: '2.0'
info:
  version: "0.1.1"
  title: test Application

paths:
  /account:
    get:
      responses:
        '200':
          description: the status of an account
          schema:
            properties:
              account:
                $ref: '#/definitions/Account'

definitions:
  $ref: 'testdef.yml'
